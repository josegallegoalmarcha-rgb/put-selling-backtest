name: Run Master Layer1 Diagnostic

on:
  push:
    branches: [main]
    paths:
      - "master_layer1_diagnostic.py"
      - ".github/workflows/master_layer1_diagnostic.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  diagnostic:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install pandas numpy scipy statsmodels

      - name: Run master Layer-1 diagnostic
        run: python master_layer1_diagnostic.py

      - name: Commit diagnostic results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add results/
          if git diff --cached --quiet; then
            echo "No result changes to commit"
          else
            git commit -m "Add master Layer-1 diagnostic"
            git push
          fi
